# Setup

1. Install dependencies with `npm i`
2. Copy and configure `.env.template` to `.env`
3. Run `npm run dev`

## To compile server endpoints to react-query hooks

Run `npm run compile` while the server is running in dev mode.
